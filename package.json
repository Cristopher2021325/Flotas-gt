{
  "name": "flotasegura-gt",
  "version": "1.0.0",
  "description": "proyecto de practica: sistema flotasegura gt (menu de consola en typescript)",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc && node scripts/copy-json.js",
    "start": "node dist/index.js",
    "api": "ts-node-dev --respawn --transpile-only src/index.ts api"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.9",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.3"
  }
}
